import json
import re
import random_responses

# Load JSON Data

def load_json(file):
    with open(file) as bot_responses:
        print(f"Loaded '(file)' succesfully!")
        return json.load(bot_responses)

get-response() for response in response_data if required_score
